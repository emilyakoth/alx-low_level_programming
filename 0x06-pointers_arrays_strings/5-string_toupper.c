#include "main.h"

/**
 * string_toupper - changes lowercase to uppercase
 * @n: the string
 * Return: char
 */
char *string_toupper(char *)
{
	int a = 0;

	while (n[a])
	{
		if (n[a] >= 97 && n[a] <= 122)
		{
			n[a] -= 32
		}
		a++;
	}

	return (n);
}
