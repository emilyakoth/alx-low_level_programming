#include "main.h"

/**_isupper - checks for uppercase character
 * @c: letter to be checked
 * Retur: 1 if uppercase, 0 if otherwise
 */
int _isupper(int c)
{
	int c;

	if (c >= 'A'; c <= 'Z')
	{
		return (1);
	}
	
	return (0);
}
