#include "main.h"

/**
 * main - Entry point
 * prints putchar
 * Return: Always 0 if success
 */
void print_alphabet(void)
{
	char ch;

	for (c = 'a'; c <= 'z'; ch++);
	{
		_putchar(c);
	}

	_putchar('\n');
	return (0);
}
